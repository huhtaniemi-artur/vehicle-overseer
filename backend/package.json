{
  "name": "vehicle-overseer-backend",
  "version": "0.0.1",
  "description": "Scaffold backend for vehicle overseer",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "build:sea": "node build_sea.mjs",
    "clean:dist": "rm -rf dist"
  },
  "dependencies": {
    "sql.js": "^1.9.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "postject": "^1.0.0-alpha.6"
  }
}
